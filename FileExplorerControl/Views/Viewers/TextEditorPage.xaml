<Page
    x:Class="kurema.FileExplorerControl.Views.Viewers.TextEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kurema.FileExplorerControl.Views.Viewers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vc="using:kurema.FileExplorerControl.Helper.ValueConverters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >
    <Page.Resources>
        <vc:BoolToEnumConverter x:Key="vc_b2e"/>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <MenuBar>
            <MenuBarItem Title="File">
                <MenuFlyoutItem Text="Open" IsEnabled="{x:Bind CanChageSavePosition,Mode=OneWay}" />
                <MenuFlyoutItem Text="Save" IsEnabled="{x:Bind CanOverwrite,Mode=OneWay}" />
                <MenuFlyoutItem Text="Save As..." IsEnabled="{x:Bind CanChageSavePosition,Mode=OneWay}" />
                <MenuFlyoutItem Text="Save Copy As..." />
                <MenuFlyoutItem Text="Reload" />
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutSubItem Text="Convert">
                    <MenuFlyoutItem Text="UPPER CASE"/>
                    <MenuFlyoutItem Text="lower case"/>
                    <MenuFlyoutItem Text="UPPER CASE (U.S.)"/>
                    <MenuFlyoutItem Text="lower case (U.S.)"/>
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="全角ひらがな" Visibility="{x:Bind IsJapanese,Converter={StaticResource vc_b2e},ConverterParameter=Visible:Collapsed}" />
                    <MenuFlyoutItem Text="全角カタカナ" Visibility="{x:Bind IsJapanese,Converter={StaticResource vc_b2e},ConverterParameter=Visible:Collapsed}" />
                </MenuFlyoutSubItem>
            </MenuBarItem>
        </MenuBar>
        <TextBox VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1" AcceptsReturn="True" IsSpellCheckEnabled="{x:Bind IsSpellCheckEnabled,Mode=OneWay}"
                 ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.VerticalScrollMode="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto"
                 >

        </TextBox>
    </Grid>
</Page>
