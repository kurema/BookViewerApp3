<UserControl
    x:Class="kurema.FileExplorerControl.Views.FileExplorerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:kurema.FileExplorerControl.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:kurema.FileExplorerControl.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <TreeView CanReorderItems="False" CanDragItems="False" x:Name="treeview" Grid.Column="0" SelectionMode="Single" IsTapEnabled="True" Expanding="TreeView_Expanding">
            <TreeView.ItemTemplate>
                <DataTemplate>
                    <TreeViewItem HasUnrealizedChildren="True" >
                        <StackPanel Orientation="Horizontal">
                            <Image Height="20" Margin="4"
                                Source="/FileExplorerControl/res/icon_folder_s.png" Stretch="UniformToFill" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Content.Title}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </TreeViewItem>
                </DataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
        <local:FileExplorerContentControl x:Name="content" Grid.Column="1"/>
    </Grid>
</UserControl>
