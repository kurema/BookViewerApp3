<Page
    x:Class="BookViewerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookViewerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
    <SemanticZoom HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Background="Black">
        <SemanticZoom.ZoomedInView>
            <GridView HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible"
                        ZoomMode="Enabled" MinZoomFactor="0.5" MaxZoomFactor="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  Background="Aqua">
                        <Image Source="./Assets/Wide310x150Logo.scale-200.png" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Stretch="UniformToFill" />
                    </ScrollViewer>
                </GridView>
        </SemanticZoom.ZoomedInView>
        <SemanticZoom.ZoomedOutView>
            <GridView>
                <TextBlock Text="Yesssssssssssssssssssssssss"/>
            </GridView>
        </SemanticZoom.ZoomedOutView>
    </SemanticZoom>
        <CommandBar ClosedDisplayMode="Compact">
            <AppBarButton Label="適当なボタン" Icon="Edit"/>
        </CommandBar>
    </Grid>
</Page>
